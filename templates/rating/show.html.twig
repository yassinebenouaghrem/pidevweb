{% extends 'base.html.twig' %}

{% block title %}Rating{% endblock %}

{% block body %}
    <h1>Rating</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ rating.getId() }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ rating.getEmail() }}</td>
            </tr>
            <tr>
                <th>Rating</th>
                <td>{{ rating.getRating() }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('rating_index') }}">back to list</a>

    <a href="{{ path('rating_edit', {'id': rating.getId()}) }}">edit</a>

    {{ include('rating/_delete_form.html.twig') }}
{% endblock %}

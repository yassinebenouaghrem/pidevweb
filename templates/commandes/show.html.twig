{% extends 'base.html.twig' %}

{% block title %}Commandes{% endblock %}

{% block body %}
    <h1>Commandes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdCommande</th>
                <td>{{ commande.idCommande }}</td>
            </tr>
            <tr>
                <th>IdPanier</th>
                <td>{{ commande.idPanier }}</td>
            </tr>
            <tr>
                <th>IdProduit</th>
                <td>{{ commande.idProduit }}</td>
            </tr>
            <tr>
                <th>Quantitee</th>
                <td>{{ commande.quantitee }}</td>
            </tr>
            <tr>
                <th>PrixU</th>
                <td>{{ commande.prixU }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('commandes_index') }}">back to list</a>

    <a href="{{ path('commandes_edit', {'idCommande': commande.idCommande}) }}">edit</a>

    {{ include('commandes/_delete_form.html.twig') }}
{% endblock %}

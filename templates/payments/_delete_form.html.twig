<form method="post" action="{{ path('payments_delete', {'idPayment': payment.idPayment}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ payment.idPayment) }}">
    <button class="btn">Delete</button>
</form>

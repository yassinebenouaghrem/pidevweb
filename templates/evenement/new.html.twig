{% extends 'baseBack.html.twig' %}

{% block title %}Evenement{% endblock %}

{% block container %}
    <h1>Create new Evenement</h1>

    {{ include('evenement/_form.html.twig', {'button_label': 'Save'})  }}

    <a href="{{ path('evenement_index') }}">back to list</a>
{% endblock %}

{% extends 'base.html.twig' %}

{% block title %}Notifications index{% endblock %}

{% block body %}
    <h1>Notifications index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Etat</th>
                <th>Date</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for notification in notifications %}
            <tr>
                <td>{{ notification.id }}</td>
                <td>{{ notification.etat }}</td>
                <td>{{ notification.date ? notification.date|date('Y-m-d H:i:s') : '' }}</td>
                <td>
                    <a href="{{ path('notifications_show', {'id': notification.id}) }}">show</a>
                    <a href="{{ path('notifications_edit', {'id': notification.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('notifications_new') }}">Create new</a>
{% endblock %}

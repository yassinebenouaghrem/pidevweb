
<form method="post" action="{{ path('therapeute_delete', {'id': therapeute.getId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ therapeute.getId) }}">
    <button class="btn">Delete</button>
</form>
